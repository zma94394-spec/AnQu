# 开发环境默认使用内置 Mock 数据，后端未启动也能完整预览界面。
# 界面右上角会显示 MOCK 标记，避免把演示数据误当成真实数据。
#
# 接后端联调时改为 false（或直接删掉这一行），
# 请求会经 vite.config.ts 的 proxy 转发到 http://127.0.0.1:3000
VITE_USE_MOCK=true

# 仅当后端与前端不同域、且不走 proxy 时才需要显式指定
# VITE_API_BASE_URL=http://127.0.0.1:3000
